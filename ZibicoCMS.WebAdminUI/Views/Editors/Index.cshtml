
@{
    ViewBag.Title = "Editör Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model  List<Editors>
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default card-view">
            <div class="panel-heading">
                <div class="pull-left">
                    <h6 class="panel-title txt-dark">
                        Editör Listesi
                    </h6>
                </div>
                <div class="clearfix"></div>
            </div>
            <div class="panel-wrapper collapse in">
                <div class="panel-body">
                    <a class="btn btn-success" href="/Editors/Create" data-toggle="tooltip" title="Yeni"><i class="zmdi zmdi-account-add"></i> Yeni Editör Ekle</a>
                    <div class="table-wrap">
                        @if (Request.QueryString["Status"] == "DeleteSuccess")
                        {
                            <div class="alert alert-warning mt-25">Editör bilgisi başarıyla silindi.</div>
                        }
                        <div class="table-responsive">
                            <table id="staffTable" class="table table-hover display  pb-30">
                                <thead>
                                    <tr>
                                        <th>İsim</th>
                                        <th>Kullanıcı Adı</th>
                                        <th>Email</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tfoot>
                                    <tr>
                                        <th>İsim</th>
                                        <th>Kullanıcı Adı</th>
                                        <th>Email</th>
                                        <th></th>
                                    </tr>
                                </tfoot>
                                <tbody>
                                    @foreach (Editors item in Model)
                                    {
                                        <tr>
                                            <td>@item.Firstname @item.Lastname</td>
                                            <td>@item.Nickname</td>
                                            <td>@item.Email</td>
                                            <td>
                                                <a class="btn btn-default btn-xs" href="/Editors/Create?ID=@item.ID" data-toggle="tooltip" title="Detay"><i class="zmdi zmdi-search"></i> Detay</a>
                                                <a class="btn btn-danger btn-xs" href="/Editors/Delete/?ID=@item.ID" data-toggle="tooltip" title="Sil"><i class="zmdi zmdi-delete"></i> Sil</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

